# 刀模图在线生成平台 · 开发文档

> 版本：v1.0 ｜ 日期：2026-09-14
> 定位：面向包装/印刷行业的**参数化刀模图（Dieline）在线生成工具**，输入盒型与尺寸，实时生成 2D 展开图、3D 折叠预览，并导出生产可用的矢量文件。

---

## 目录

1. [竞品分析](#1-竞品分析)
2. [产品定位与功能规划](#2-产品定位与功能规划)
3. [技术选型](#3-技术选型)
4. [系统架构](#4-系统架构)
5. [核心模块设计](#5-核心模块设计)
6. [关键算法与几何推导](#6-关键算法与几何推导)
7. [数据模型](#7-数据模型)
8. [页面结构与路由](#8-页面结构与路由)
9. [开发路线图](#9-开发路线图)
10. [部署方案](#10-部署方案)
11. [风险与注意事项](#11-风险与注意事项)

---

## 1. 竞品分析

### 1.1 总览

| 站点 | 形态 | 技术栈 | 盒型库 | 3D | 导出格式 | 商业模式 |
|---|---|---|---|---|---|---|
| geomlab.pro 几何实验室 | 静态多页（MPA） | 原生 JS + Three.js + i18n + 鉴权 | **800+**（FEFCO/ECMA/POS/礼盒/内件） | Three.js 折叠/装配/分解动画 | SVG、DXF、CF2、DES、PDF、AI；**支持导入 DXF/DES** | 账号/专业版 |
| zhangdezhi1.gicp.fun | SPA | Vite + React | 26 种（外箱 RSC、飞机盒、快递箱、E 瓦盒） | 无 | DXF、PDF（可带尺寸标注） | 免费 + 微信引流 |
| ds.mxder.com Packform | SPA + PWA | Vite + React + vite-plugin-pwa | ECMA 折叠纸盒 | **2D/3D 同步 + 版面设计**（字体、品牌排版、各面独立颜色） | SVG、PNG、PDF proof、印刷预检 | 免费 + SEO 获客 |
| tools.shengfangfurniture.com | SPA + PWA | Vite + React + react-router + Zustand + Lucide + 自研 Service Worker | 以 0201 为主 | 无 | DXF、PDF、AI | 免费 + 赞助（含赞助墙） |
| templatemaker.nl | 服务端渲染 MPA | **PHP**（action.php 表单 POST）+ Matomo | 40+ 创意纸艺模板（纸袋、天地盒、金字塔、多面体、心形盒等） | 无 | PDF、SVG、DXF | 免费 + 捐赠 |

### 1.2 各站实现路线详解

#### geomlab.pro（几何实验室）— 盒型库深度路线
- **架构**：服务端输出静态 HTML，`studio.html` 一页承载全部交互；通过 `<script type="importmap">` 直接映射本地 `node_modules` 的 Three.js ESM 模块（非打包工具产物），说明前端零构建、模块直载。
- **2D 展开**：Canvas 2D 渲染，支持滚轮缩放、拖拽平移、实体点选（"Click an entity on the canvas"）、圆角工具、对比视图。
- **3D 预览**：Three.js，提供折叠（fold 0–100%）、装配（assemble）、分解（explode 0–80%）三个滑块做动画插值——本质是把展开图面片沿压痕线做铰链变换。
- **国际化**：`i18n-boot.js` / `i18n-init.js` 双语（中/英），HTML 用 `data-i18n` 属性标注。
- **鉴权**：`auth-guard.js`，部分功能需登录。
- **盒型组织**：按标准目录树分组——FEFCO 02xx 开槽箱 / 03xx 天地盖 / 04xx 折盘托盘 / 05xx 裹包盒 / 06xx 滑盖盒 / 07xx 自锁底 / 09xx 隔板内件；ECMA A/B/C/D/E/F 系列；POS 陈列展架系列；每类还有 "DES 目录"（DES 是盒型图纸交换格式）。
- **启示**：护城河在盒型库数量与几何正确性，不在框架。

#### zhangdezhi1.gicp.fun — 工艺参数路线
- **架构**：Vite + React 单页应用，百度统计埋点，部署在花生壳内网穿透域名（个人项目特征）。
- **特色**：工艺参数极细——糊口、开槽、加深、长缩、端缩（末端插入边）、糊口斜切、糊口根部圆角；提供"内外尺寸和压线尺寸换算"；支持**双联刀模**（一版两件拼版，直接服务于飞机盒/快递箱打样）。
- **导出**：DXF / PDF，均可选"带尺寸标注"版本。
- **启示**：懂行的是"工艺参数"，默认值体系直接决定工具是否可用。

#### ds.mxder.com（Packform）— 设计体验路线
- **架构**：Vite + React + vite-plugin-pwa（manifest + registerSW），可离线使用，做成"软件感"的 Web App；SEO 极强——JSON-LD 结构化数据（SoftwareApplication + FAQPage）、多 landing 页（/free-box-design/、/box-dieline-generator/ 等长尾词）。
- **特色**：
  - 2D 刀版与 3D 折弯**同步**，折弯半径按纸厚（0.5mm）自动计算；
  - **版面设计**：品牌标题、产品描述、主色，内置思源黑体/宋体、Inter、Oswald、Bebas、Playfair 等 OFL 可商用字体，对象级属性面板（字号/字重/颜色/位置/旋转/透明度），应用范围限定到"正面/背面/侧面"等具体盒面；
  - 3D 场景渲染：各面独立设置颜色、材质（白卡纸）、地面朝向、光影模式；
  - 项目管理："最近项目"列表、结构检查（"结构检查通过"）、计价尺寸。
- **启示**：2D+3D+版面三合一，是差异化体验最强的路线；SEO 结构化数据值得复用。

#### tools.shengfangfurniture.com — 物流与成本路线
- **架构**：Vite + React，手动分包（`vendor-router` / `vendor-zustand` / `vendor-lucide`）→ 即 react-router + Zustand + Lucide 图标；**自研 Service Worker**（区分本地开发/生产、controllerchange 自动刷新、缓存清理），PWA。
- **特色**：
  - 尺寸类型四选一：制造尺寸 / 内尺寸 / 外尺寸 / **内装物计算**（输内装物自动推荐箱尺寸）；
  - 材质库：B/E/C/A 瓦楞（3 层）、EB/BC/AB（5 层）、自定义厚度 0.2–10mm；
  - 展开信息面板：制造尺寸、展开总宽/总高、摇盖 F1/F2 实时回显；
  - **成本估算/报价**、**托盘装箱**（中国/欧标/澳标/美标/日标托盘，输出每层箱数、层数、利用率、每托总数）、**集装箱装箱**（20GP/40GP/40HQ/45HQ，输出摆放坐标、体积利用率、整柜箱数）、**堆码/承重计算**、**合规/运输建议**；
  - 线型图例：裁切线/压痕线/折痕线三色区分。
- **启示**：把"刀模"延伸到"包装工程"，物流计算是高频刚需且竞品少。

#### templatemaker.nl — 创意模板路线
- **架构**：经典 LAMP——每个模板一个静态页 + 参数表单，`POST` 到 `assets/php/action.php` 服务端生成文件返回下载；Matomo 自托管统计；多语言（/zh/）。
- **特色**：模板全部原创（手推几何），覆盖纸袋、天地盒、蛋糕盒、棺材盒、圆台、蛋形盒、心形盒、柏拉图立体、索马立方、星盒等 40+；每模板附详细图文说明与参数解释；提供"纸尺寸微调"（适配 A4/A3 打印）。
- **启示**：内容即 SEO；参数说明文案是信任资产；服务端生成反而简单可靠。

### 1.3 功能对比矩阵

| 能力 | geomlab | zdz | Packform | sff | templatemaker |
|---|:-:|:-:|:-:|:-:|:-:|
| 参数化尺寸输入 | ✅ | ✅ | ✅ | ✅ | ✅ |
| 纸厚/材质影响几何 | ✅ | ✅ | ✅ | ✅ | ⚠️ |
| 内/外/制造尺寸换算 | ✅ | ✅ | ⚠️ | ✅ | ❌ |
| 2D 展开实时渲染 | ✅ | ✅ | ✅ | ✅ | 服务端 |
| 3D 折叠预览 | ✅ | ❌ | ✅ | ❌ | ❌ |
| 版面/平面设计 | ❌ | ❌ | ✅ | ❌ | ❌ |
| DXF 导出 | ✅ | ✅ | ❌ | ✅ | ✅ |
| PDF 导出 | ✅ | ✅ | ✅ | ✅ | ✅ |
| SVG 导出 | ✅ | ❌ | ✅ | ❌ | ✅ |
| AI/CF2/DES 导出 | ✅ | ❌ | ❌ | ✅(AI) | ❌ |
| 导入（DXF/DES） | ✅ | ❌ | ❌ | ❌ | ❌ |
| 装箱/托盘计算 | ❌ | ❌ | ❌ | ✅ | ❌ |
| 成本估算 | ❌ | ❌ | ⚠️ | ✅ | ❌ |
| 账号/云项目 | ✅ | ❌ | ✅(本地) | ❌ | ❌ |
| PWA/离线 | ❌ | ❌ | ✅ | ✅ | ❌ |
| 多语言 | ✅ | ❌ | ❌ | ❌ | ✅ |

### 1.4 结论与差异化机会

1. **所有竞品的核心引擎都是同一条路线**：`参数 → 纯几何计算 → 折线/切线实体 → 矢量渲染 → 多格式导出`，全部可纯前端完成，无后端依赖（templatemaker 例外，PHP 服务端生成）。
2. **盒型库广度**（geomlab）与**体验深度**（Packform）与**工程维度**（sff）三者没人同时占齐 → 新站的机会：**中等盒型库 + 2D/3D 同步 + 装箱报价一体**。
3. 行业通用格式是 **DXF（CAD 下料）+ PDF（打样校对）+ SVG（Web 预览）**，AI/CF2/DES 为加分项。
4. 中文市场获客靠 SEO（结构化数据）+ 微信私域，二者成本极低。
5. 免费策略已是行业默认，变现靠专业版（账号/云项目/去水印）。

---

## 2. 产品定位与功能规划

### 2.1 产品定位

**"刀模图（盒型展开图）在线生成器"**：面向纸箱厂业务员、包装设计师、印刷打样师、电商卖家，输入盒型 + 尺寸 + 工艺参数，10 秒出可生产的刀模图。

### 2.2 目标用户与场景

| 用户 | 场景 | 核心诉求 |
|---|---|---|
| 纸箱厂业务员 | 客户报价、打样 | 快速出图 + 尺寸换算 + 报价 |
| 包装设计师 | 结构设计 | DXF 进 AI 继续设计、3D 预览 |
| 印前/制版 | 刀模文件 | 线型规范（切/压分层）、CF2 |
| 电商卖家 | 定制快递箱 | 直观、防呆、能看 3D |

### 2.3 功能规划

**P0（MVP 必须）**
- 盒型：0201 开槽箱、0427 飞机盒、0200/0215 常用箱、自锁底 0700、天地盖（盒+盖）
- 参数：L/W/H、纸厚（材质库：E/B/C/A/EB/BC/AB + 自定义）、糊口、摇盖修正、内外/制造尺寸换算
- 2D 渲染：裁切线（红实线）/压痕线（蓝虚线）/折痕（点划线）分层显示、缩放平移、尺寸标注、显示实际占用幅面
- 导出：SVG、DXF（R12，分图层）、PDF（1:1 毫米矢量）
- 参数持久化：URL 查询串 + localStorage

**P1（体验增强）**
- 3D 折叠预览（Three.js，折叠滑块 0–100%）
- 展开信息面板（总宽/总高、摇盖 F1/F2、面积）
- 盒型库扩到 30–50 种（FEFCO 02xx/04xx/07xx 高频 + ECMA 插口盒）
- 双联/多联拼版（一版多件）
- PWA 离线可用

**P2（工程与商业）**
- 托盘/集装箱装箱计算 + 成本估算
- 版面设计（盒面文字/LOGO 排版，2D/3D 同步）
- 账号系统 + 云端项目 + 历史版本
- DXF 导入编辑、CF2/DES 导出

### 2.4 非功能需求

- 单位严格 mm，内部浮点运算，导出保留 3 位小数；
- 参数变更到重绘 < 100ms（几何计算量极小，性能不是瓶颈）；
- 移动端可用（参数面板可折叠），精确编辑引导到桌面端；
- SEO：每个盒型一个静态着陆页 + JSON-LD（SoftwareApplication + FAQPage）。

---

## 3. 技术选型

> 原则：对齐主流竞品（Vite + React 系），纯前端优先，几何引擎与渲染解耦。

| 层 | 选型 | 理由与来源 |
|---|---|---|
| 构建 | **Vite 7** | 4/5 家竞品用 Vite 或类零构建；`vendor-router` 式手动分包可选 |
| UI 框架 | **React 19 + TypeScript** | sff/Packform/zdz 同栈；生态组件最全 |
| 状态管理 | **Zustand** | sff 同款（vendor-zustand）；几何结果用 `useSyncExternalStore` 派生 |
| 路由 | react-router（P2 起需要） | MVP 单页可不用路由 |
| 图标 | lucide-react | sff 同款 |
| 2D 渲染 | **Canvas 2D**（自绘） | geomlab 同款；万级线段无压力；SVG DOM 仅用于导出 |
| 3D 预览 | **Three.js**（`three` + addons） | geomlab 同款；折叠动画用铰链变换 |
| 导出-SVG | 自拼 SVG 字符串 | 实体模型在手，直接序列化 |
| 导出-DXF | 自拼 **DXF R12 ASCII** | 无依赖、兼容性最好（R12 所有 CAD 识别）；LINE/ARC/CIRCLE + LAYER |
| 导出-PDF | **pdf-lib** | 纯前端矢量画线；比 jsPDF 线条控制精确；嵌入中文字体子集（fontkit） |
| 表单 | 受控组件 + 原生 input | 参数面板简单，无需表单库 |
| 样式 | Tailwind CSS 4 或原生 CSS | 竞品多为手写 CSS；按团队习惯 |
| PWA | vite-plugin-pwa（Workbox） | Packform 同款；比 sff 自研 SW 省心 |
| 统计 | 百度统计（国内）+ 可选 Umami 自托管 | 中文竞品标配 |
| 后端（P2+） | 轻量：Cloudflare Workers / 腾讯云函数 + KV/Supabase | 仅账号、云项目、分享链接用；MVP 零后端 |
| 部署 | 静态托管：Vercel / 腾讯云 EdgeOne / OSS+CDN | 纯静态，成本近零 |

**明确不引入**：OpenSCAD/CGAL（盒型是平面折线几何，不需要实体布尔）；重型 CAD 库（paper.js 可选但非必须——自绘可控性更强）。

---

## 4. 系统架构

### 4.1 架构图

```
┌──────────────────────────── 浏览器（纯前端 SPA）────────────────────────────┐
│                                                                             │
│  参数面板(React)          盒型库注册表                    导出面板            │
│  L/W/H·纸厚·工艺   ──▶  BoxRegistry: {                     ──▶ SVG 序列化   │
│  Zustand store            'fefco-0201': builder()          ──▶ DXF R12     │
│         │                 'fefco-0427': builder()          ──▶ PDF(pdf-lib)│
│         ▼                 'ecma-a0201': builder() ...                    │
│  ┌───────────────── 几何引擎（纯函数，框架无关）──────────────────┐          │
│  │ buildDieline(params) -> DielineResult                        │          │
│  │   entities: Line/Arc/Circle  (layer: cut/crease/perf/slug)   │          │
│  │   panels:  Panel3D[]  (2D多边形 + 折叠父子关系 + 铰链轴)      │          │
│  │   meta: 展开幅面/面积/换算后制造尺寸/校验警告                 │          │
│  └──────┬────────────────────────────────┬─────────────────────┘          │
│         ▼                                ▼                                  │
│  Canvas2D 渲染器                 Three.js 渲染器                            │
│  (缩放/平移/点选/标注/            (面片网格 + 铰链层级 +                     │
│   线型分层/幅面框)                 折叠/分解动画插值)                        │
│         │                                │                                 │
│         └───── URL Query / localStorage / (P2)云端项目 ──────┘               │
└─────────────────────────────────────────────────────────────────────────────┘
                              （P2 可选轻后端）
                 Cloudflare Workers / Supabase：账号·项目云同步·分享短链
```

### 4.2 数据流（单向）

```
用户改参数 → store 更新 → buildDieline(params) 纯函数重算（<1ms）
  → 2D/3D 渲染器各自订阅重绘 → 导出时从同一 DielineResult 序列化
```

关键点：**几何引擎是纯函数库**（可独立 npm 包），React 只是壳。这样引擎可单元测试快照对拍（同一组参数 → 同一组实体），也为未来 WASM/服务端复用留口。

---

## 5. 核心模块设计

### 5.1 盒型参数化引擎（核心）

每个盒型实现为一个 **builder 纯函数**，注册到统一接口：

```ts
// 引擎接口（框架无关）
interface BoxBuilder {
  id: string;                       // 'fefco-0201'
  name: string;                     // '0201 开槽箱'
  category: 'fesco-02xx' | 'fesco-04xx' | 'fesco-07xx' | 'ecma' | ...;
  params: ParamSpec[];              // 参数面板的声明式描述
  build(p: ResolvedParams): DielineResult;
}

interface ParamSpec {
  key: 'L' | 'W' | 'H' | 't' | 'glueFlap' | ...;
  label: string;
  type: 'length' | 'number' | 'enum' | 'bool';
  default?: number;
  min?: number; max?: number;
  dependsOn?: { key: string; when: any }[];   // 条件显隐
  auto?: (p: ResolvedParams) => number;        // 自动推导（如摇盖=W/2）
}
```

`ResolvedParams` 里先完成**尺寸体系换算**（见 6.1），builder 拿到的永远是"制造尺寸 + 工艺参数"。

`DielineResult` 输出三类内容：

1. `entities`：2D 实体（折线段、圆弧、圆孔），带图层（`cut` 裁切 / `crease` 压痕 / `perf` 齿刀 / `slug` 废料边 / `dim` 标注）；
2. `panels`：3D 面片树（每个面 = 多边形 + 相对父面的折线轴 + 折叠角 90°），由同一几何推导生成，保证 2D/3D 一致；
3. `meta`：展开总宽/总高、净面积、校验结果（如"摇盖间隙不足""糊口小于 15mm 警告"）。

**坐标与单位约定**：全局 mm、y 轴向下（与 SVG/Canvas 一致，导出 DXF 时统一翻转为 CAD 习惯的 y-up）；原点在展开图左上角；所有数值保留 3 位小数。

### 5.2 2D 渲染模块（Canvas）

| 能力 | 实现要点 |
|---|---|
| 线型规范 | `cut`：1.5px 红实线；`crease`：1px 蓝/黑虚线 `dash [6,3]`；`perf`：1px 点划线；颜色可在设置里切换"打印模式"（全黑） |
| 视图 | 滚轮缩放（以光标为中心）、空格拖拽平移、`⤢ 适应视图`（geomlab 同款交互） |
| 标注 | 可开关；尺寸线 + 箭头 + 文本，标注层单独控制 |
| 幅面框 | 显示纸板幅面/展开占用矩形 + 面积 |
| 点选 | 实体命中检测（线段距离阈值），为 P2 编辑做预留 |
| 高 DPI | `devicePixelRatio` 缩放，避免 0.5px 模糊 |

### 5.3 3D 预览模块（Three.js）

- 每个 `Panel` 生成 `THREE.Shape` → `ShapeGeometry`（双面材质），沿铰链轴建立父子 Object3D 层级；
- 折叠动画：`fold ∈ [0,1]` 插值每条折痕角度 `0° → 90°/−90°`（geomlab 的 fold 滑块同款思路）；
- 分解视图（explode）：整体沿法向/重心径向偏移；
- 材质：`MeshStandardMaterial`，可设纸板颜色、双面、边线（EdgesGeometry 描折痕）；
- 相机：OrbitControls，地面阴影可选；
- **性能**：单盒 < 100 面片，无需 InstancedMesh，任何设备 60fps。

### 5.4 导出模块

**SVG**
```xml
<svg xmlns="..." width="{W}mm" height="{H}mm" viewBox="0 0 {W} {H}">
  <g id="cut" stroke="#FF0000" fill="none" stroke-width="0.3">
    <path d="M x1 y1 L x2 y2 L ..."/>   <!-- 同图层实体合并 path -->
  </g>
  <g id="crease" stroke="#0000FF" stroke-dasharray="6,3">...</g>
</svg>
```
- 单位直接用 mm 数值（1 user unit = 1mm），印刷/AI 直接打开。

**DXF（R12 ASCII，最小可靠子集）**
```
0 SECTION / 2 ENTITIES
  0 LINE / 8 (图层名) / 10,20 x1,y1 / 11,21 x2,y2     ← 每段折线
  0 ARC  / 8 图层 / 10,20 圆心 / 40 半径 / 50,51 起止角  ← 圆角/圆孔
  0 CIRCLE / 8 图层 / 10,20 圆心 / 40 半径               ← 穿孔
0 ENDSEC / 0 EOF
```
- 图层名：`CUT`、`CREASE`、`PERF`、`DIM`（制版厂按图层分刀/压线，行业惯例）；
- y 轴翻转（CAD y-up）；R12 编码用 UTF-8 安全的图层名，避免 2000+ 版本表节复杂度。

**PDF（pdf-lib）**
- 页面尺寸 = 展开幅面（向上取整到 mm），线宽 0.3pt 起，图层同色规范；
- 页脚可选"尺寸/结构仅供参考"免责声明（竞品标配）；
- 中文字体：嵌入思源黑体子集（fontkit subset，文件 < 500KB）。

### 5.5 盒型库管理

```
src/boxes/
  fefco-0201/   index.ts  params.ts  geometry.ts  thumb.svg  README.md(图文说明)
  fefco-0427/   ...
```
- 每盒型独立目录：几何 builder + 参数声明 + 缩略图 + 图文说明（**templatemaker 的说明文案路线，SEO 资产**）；
- `BoxRegistry` 聚合并生成：分类导航、盒型搜索、"全部盒型"着陆页（静态预渲染 `vite-plugin-ssr` 或构建期生成 HTML，兼顾 SEO）。

### 5.6 持久化与分享

- 参数 → URL query（`?box=0201&L=600&W=400&H=230&t=3`），复制链接即分享；
- localStorage 存最近 20 个项目（Packform"最近项目"同款）；
- P2：云项目表（JSON 存整份 `ResolvedParams` + 版本号）。

---

## 6. 关键算法与几何推导

### 6.1 三种尺寸体系换算（瓦楞箱）

设纸厚 `t`（E 楞 2 / B 楞 3 / C 楞 3.5 / A 楞 5 / EB 5 / BC 6 / AB 8 mm）：

```
制造尺寸 = 内尺寸 + t            （长宽方向各加一个纸厚）
外尺寸   = 制造尺寸 + 2t          （对面两层纸）
0201: 摇盖 F = W制造 / 2          （对折正好盖满箱口）
开槽宽 s ≈ t + 1~2mm             （摇盖插入余量，可调）
```

输入"内装物尺寸"时（sff 的"内装物计算"）：内尺寸 = 内装物 + 单边 3–5mm 间隙，再走上式。

### 6.2 0201 开槽箱展开推导（示例实现）

```
展开总宽  = 2L' + 2W' + g          L'=W'=制造尺寸, g=糊口(默认 max(30, H/3))
展开总高  = H' + 2F                H'=制造高, F=W'/2 摇盖
面板序列（从左到右）: [糊口g][侧面W'][正面L'][侧面W'][正面L'] 每列上/下各接摇盖F
开槽：摇盖与侧翼之间留缝 s（在 W' 面板处切通），侧翼深 = F
几何特征：
  - 裁切段：外轮廓 + 开槽缝 + 摇盖轮廓
  - 压痕段：所有面板分界竖线 + 摇盖折线（水平）
  - 圆角：摇盖肩部小圆角 R2~3（默认，可关）
```

实现即"游标绘制"：维护当前 x 游标，逐面板画竖向压痕线，顶/底按摇盖轮廓走折线段，糊口侧边斜切（45° 收角 9mm，zdz 同款参数）。

### 6.3 折叠动画（铰链变换）

```
面片树：body(根, 不动)
 ├─ front(L')     沿 x=g 折 90°
 │   ├─ flapTop    沿自身顶边折 θ(t)
 │   └─ flapBottom ...
 ├─ side(W')      沿 front 边折 90°
 │   └─ ...
└─ glueFlap
```
- 每个节点在自身局部系存"折线轴 + 面多边形"；
- 动画：`angle = 90° × foldProgress`，层级矩阵自动级联（Three.js 父子即天然实现）；
- 折叠角方向由折线在实体模型上的 `direction: +1/-1` 标记决定，builder 生成实体时同步给出。

### 6.4 托盘/集装箱装箱（P2，启发式即可）

- 单箱占地 `L'×W'`，允许 90° 旋转，托盘面 `P×Q` 贪心行列排布（先试横排/竖排/混合取优）；
- 层数 = `floor(堆码限高 / 箱高)`（可选承重约束：`层数 ≤ 楼板承重/单箱重`）；
- 集装箱：按 40HQ 等内尺寸，先托盘再散箱两种模式，输出摆放示意 + 体积利用率；
- 不必上真 3D-Bin-Packing 求解器，竞品（sff）也是行列启发式，够用。

### 6.5 成本估算（P2）

```
用纸面积 = 展开总宽 × 总高 × 损耗系数(1.05~1.15)
板材价   = 面积/1000² × 板材吨价折算（按 g/m² 换算）
 + 印刷 + 模切工费（按版式）+ 糊箱工费
```
报价系数全部做成后台可配（后端仅一张 KV 配置表），先给"参考价"而非实价（竞品均如此并加免责声明）。

---

## 7. 数据模型

```ts
type Layer = 'cut' | 'crease' | 'perf' | 'slug' | 'dim';

interface Entity { layer: Layer }                 // 判别联合
interface LineEnt extends Entity { kind: 'line'; a: Pt; b: Pt }
interface ArcEnt  extends Entity { kind: 'arc';  c: Pt; r: number; a0: number; a1: number }
interface CircleEnt extends Entity { kind: 'circle'; c: Pt; r: number }
interface PolyEnt extends Entity { kind: 'poly'; pts: Pt[]; closed: boolean }

interface Hinge { edge: [Pt, Pt]; direction: 1 | -1; angleDeg: number } // 默认90

interface Panel {
  id: string;
  polygon: Pt[];              // 局部2D
  hinge?: Hinge;              // 相对父面板
  label?: 'front'|'back'|'side'|'top'|'bottom'|...;
}

interface DielineResult {
  entities: Entity[];          // 全局2D坐标(mm, y-down)
  panels: Panel[];            // 3D折叠树
  meta: {
    sizeType: 'inner'|'make'|'outer';
    makeSize: { l: number; w: number; h: number };
    unfoldW: number; unfoldH: number;
    flapF1: number; flapF2: number;
    area: number;              // m²
    warnings: string[];        // 工艺校验
  };
}

interface ProjectState {
  boxId: string;
  params: Record<string, number | string | boolean>;
  view: { zoomMode: 'fit'|'manual'; showDim: boolean; ... };
  design?: { texts: TextObject[]; colors: Record<string, string> };  // P2
}
```

---

## 8. 页面结构与路由

```
/                        首页（工具即首页，参数+画布布局）
/boxes                   全部盒型库（SEO 着陆，静态预渲染）
/boxes/fefco-0201        盒型详情+图文说明（SEO）→ "打开设计" 进工具 ?box=fefco-0201
/guides                  尺寸换算/纸板材质科普（SEO 内容矩阵）
/about · /sponsor        关于/赞助（sff 式赞赏墙）
app 内（单页内 tab，非路由）:
  [参数面板] [2D 刀版] [3D 预览] [展开信息] [导出] [装箱计算(P2)] [报价(P2)]
```

布局参考竞品通用范式：**左侧参数面板（可滚动）+ 右侧画布（自适应）+ 顶部工具条（导出/视图/复位）**，移动端参数面板收为底部抽屉。

---

## 9. 开发路线图

### Phase 1 — MVP（核心闭环）
- 工程搭建：Vite + React + TS + Zustand + Tailwind；几何引擎独立包结构
- 引擎：实体模型 + 0201/0427/0700/天地盖 4 个 builder + 尺寸换算 + 工艺参数
- 2D：Canvas 渲染（线型/缩放/平移/适应视图/标注开关）
- 导出：SVG / DXF-R12 / PDF(1:1)
- 持久化：URL + localStorage
- 部署上线 + 百度统计
- **验收**：0201 输入 600×400×230/B 楞，导出 DXF 在 AutoCAD 打开图层、尺寸正确

### Phase 2 — 体验与库扩展
- Three.js 3D 折叠/分解动画
- 盒型库扩至 30–50（FEFCO 02/04/07 高频 + ECMA 插口/反插盒）
- 展开信息面板 + 工艺校验警告
- PWA（vite-plugin-pwa）
- 盒型详情 SEO 页 + JSON-LD + FAQ
- 移动端适配

### Phase 3 — 工程增值
- 托盘/集装箱装箱计算 + 成本估算（配置化系数）
- 双联/多联拼版
- 版面设计（盒面文字/LOGO，2D/3D 同步）——对标 Packform
- 账号 + 云项目（Cloudflare Workers/Supabase）

### Phase 4 — 专业工具
- DXF 导入编辑（对标 geomlab）
- CF2 / DES / AI 导出
- 自定义盒型 DSL / 参数化保存分享
- 团队协作、报价单导出

---

## 10. 部署方案

| 项 | 方案 | 说明 |
|---|---|---|
| 国内主站 | 腾讯云 EdgeOne Pages / 阿里云 OSS+CDN | 纯静态；域名需 ICP 备案 |
| 海外/镜像 | Vercel 或 Cloudflare Pages | 零备案、自动 CI（push 即部署） |
| CI/CD | GitHub Actions：lint → test（引擎快照对拍）→ build → deploy | 几何引擎单测是质量核心 |
| 版本策略 | PWA 更新沿用 sff 思路：`controllerchange` 自动刷新 + 缓存清理 | 避免旧版本滞留 |
| 监控 | 百度统计 + Sentry（自托管可选） | — |
| 后端(P2) | Cloudflare Workers + KV（或 Supabase） | 账号/云项目/分享短链/报价系数配置 |

---

## 11. 风险与注意事项

1. **精度与免责**：所有站点均注明"尺寸/结构仅供参考，正式刀模以工厂建议为准"——必须保留同等免责声明，避免生产事故责任。
2. **盒型库版权**：FEFCO/ECMA 编号体系是公开标准，但**具体图纸几何**有著作权——竞品图纸不可逆向照搬，需自行推导几何（参数化公式本来就是自己的实现）或获得授权；templatemaker 的原创模板明确有版权声明。
3. **字体版权**：版面设计（P2）只内置 OFL/APACHE 协议字体（思源黑体/宋体、Inter 等），用户上传字体需提示授权责任。
4. **DXF 兼容性**：坚持 R12 最小集 + UTF-8 图层名；上线前用 AutoCAD/文管/ArtiosCAD 三端回归。
5. **浮点与显示**：内部全精度计算，显示四舍五入到 0.1mm，导出 3 位小数；避免"看起来 600 实际 599.999"。
6. **默认参数体系是产品力**：糊口、开槽、圆角等默认值需按行业经验设定（参考竞品：糊口 30、开槽 t+2、摇盖肩圆角 2–3、糊口斜切 9），并全部开放可调。
7. **SEO 与 SPA**：工具页无 SEO 价值，但盒型详情/科普页必须静态预渲染，复用 Packform 的 JSON-LD 结构化数据打法。
8. **备案与统计**：国内部署需备案；百度统计异步加载避免阻塞；隐私政策页。

---

> 附：本档基于对 geomlab.pro、zhangdezhi1.gicp.fun、ds.mxder.com、tools.shengfangfurniture.com、templatemaker.nl 五站的公开前端源码与页面结构实测分析（2026-09-14）。
